# Fixture B — Landing Page

Intentional regressions:

- horizontal-overflow: wide-hero 640px on mobile
- overlapping-elements: badges on pricing card
- broken-image: /missing-hero.png
- console-error: simulated
- responsive: pricing cards grid collapses but wide hero still overflows

Verify: config ignores, scenario hover, compare, CI.
